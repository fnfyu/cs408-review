# 组成原理课件精读重写规范（给子代理）

目标：把 `organized-lessons.json` 里组成原理某几节的「整理正文」从 OCR 碎片，重写成**可直接复习的完整讲义**。

## 硬性要求

1. **细读** `web/_co_chN_dump.txt` 中对应 `==== co-xxx` 段落的全部课件原文；动画重复页合并，但**新增知识点不得丢**。
2. 广告页/“仅供娱乐”番外可精简，但正式考点必须完整。整份 PDF 都是重点，不得用“考试不考”为由删减正式课件内容。
3. 有「上课手稿」与「纯净版」两套课时：两套都要重写；手稿版可多保留课堂口诀/易错提醒，纯净版以考点完整为准，二者知识点不得互相矛盾。
4. 输出**合法 JSON**，结构与 `web/content/cn/ch1.json` / `web/content/os/ch1.json` 一致：

```json
{
  "chapter": "第X章 ...",
  "chapter_order": X,
  "rewritten_at": "2026-09-10",
  "source": "细读王道课件 page_texts",
  "lessons": {
    "co-0XX": {
      "blocks": [
        {
          "heading": "标题",
          "kind": "概念与原理|规则与易错|过程与算法|对比辨析|公式/代码/计算|例题与应用|图示/结论",
          "pages": [1, 2],
          "lines": ["完整通顺的中文考点句，不要 OCR 断行碎片", "..."],
          "visual_only": false
        }
      ],
      "must_memorize": [
        { "text": "可背诵的考点句", "page": 9 }
      ]
    }
  }
}
```

5. 每个 lesson：`blocks` 建议 4–12 个（计算/流水线课可到 12–18）；`must_memorize` 建议 5–12 条；`lines` 写完整句子/要点，含 ★重点、易错、对比、公式、步骤、适用范围。
6. 图示为主的页：`visual_only: true`，但仍要写清图在讲什么、结论是什么。
7. 例题/公式块用 kind `例题与应用` 或 `公式/代码/计算`。
8. **只写你被分配的 lesson id**，写入指定输出 JSON 文件。
9. 写完后用 `JSON.parse` 自检；文件必须 UTF-8。

## 质量标准

- 不是原文粘贴，而是消化后的讲义。
- 覆盖 dump 中每一页有效知识（动画帧合并叙述即可）。
- 必记条目应是真正考点（定义、性质、步骤、公式、时序、易错对比），不是「Tips」「学习提示」之类无效句。
- 数值表示/运算类：补码规则、溢出判断、IEEE754、ALU 步骤与例题算清。
- 存储/Cache/虚拟存储：映射方式、替换、写策略、地址翻译步骤写清。
- CPU/流水线：数据相关、控制相关、冒险处理、CPI/加速比计算写清。

## 章节与 lesson 对应（注意 id 不连续）

1. 第一章 计算机系统概述：`co-001`–`co-008`
2. 第二章 数据的表示和运算：`co-029`–`co-054`
3. 第三章 存储系统：`co-017`–`co-028`
4. 第四章 指令系统：`co-074`–`co-086`
5. 第五章 中央处理器：`co-055`–`co-068`
6. 第六章 总线：`co-069`–`co-073`
7. 第七章 输入输出系统：`co-009`–`co-016`
